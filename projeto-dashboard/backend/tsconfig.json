{
  "compilerOptions": {
    /* --- Configurações Essenciais do Módulo --- */
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "target": "ES2022", // Roda em versões recentes do Node.js

    /* --- Padrões do TypeScript --- */
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    /* --- Configuração do Projeto --- */
    "outDir": "./dist", // Onde os arquivos .js compilados irão
    "rootDir": "./src", // Onde estão seus arquivos .ts
  },
  "include": ["src/**/*"], // Quais arquivos compilar
  "exclude": ["node_modules"] // O que ignorar
}
